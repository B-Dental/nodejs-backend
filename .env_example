# JWT secret used to sign JSON Web Tokens.
# Use a long, random string. Do NOT commit your real secret to version control.
# Example (do NOT use this value):
# JWT_SECRET=change-me-to-a-very-long-random-string
JWT_SECRET=

# Database connection string used by Prisma (or your DB client).
# Format depends on your database. Example for PostgreSQL:
# DATABASE_URL="postgresql://dbuser:dbpass@localhost:5432/dbname?schema=public"
# For SQLite (example):
# DATABASE_URL="file:./dev.db"
DATABASE_URL=

# Google service account private key used for server-to-server auth
# (e.g. uploading files to Google Cloud Storage). Private keys often contain
# newlines. If you store the key as a single-line environment variable,
# replace actual newlines with '\n' and when reading the env convert them back
# to real newlines (or use a secrets manager). Example placeholder:
# GOOGLE_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\nMIIEvQIBADANBgkq...\n-----END PRIVATE KEY-----\n"
GOOGLE_PRIVATE_KEY=

# Google service account client email (from the JSON key file):
# Example: my-service-account@my-project.iam.gserviceaccount.com
GOOGLE_CLIENT_EMAIL=

# NOTE: Create a local `.env` file from this template and add `.env` to
# your `.gitignore` to avoid committing sensitive values to the repository.
